{
  "config": {
    "step": {
      "user": {
        "title": "Connect Waveshare PoE Relay",
        "description": "🔌 Enter the relay board connection data.\n\nA connection test is performed automatically.",
        "data": {
          "host": "IP address",
          "port": "Modbus TCP port",
          "unit_id": "Modbus unit ID",
          "poll_interval": "Poll interval (seconds)",
          "relay_count": "Relay count"
        }
      }
    },
    "error": {
      "cannot_connect": "Connection failed - check IP, port and unit ID. Note that the board only allows one Modbus connection at a time.",
      "invalid_host": "Please enter a valid IP address."
    },
    "abort": {
      "already_configured": "This board is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure Waveshare Relay",
        "description": "Select the channel to edit. The mode can be changed at any time - name and entity ID are preserved.",
        "data": {
          "channel": "Channel"
        }
      },
      "channel": {
        "title": "Configure channel {channel_number}",
        "description": "Set the channel mode. In \"Pulse\" mode, switching on triggers a native board pulse; the board switches back off by itself. The entity ID stays unchanged.",
        "data": {
          "channel_name": "Display name",
          "betriebsart": "Mode (Switch/Pulse)",
          "impulsdauer_ms": "Pulse duration (ms)"
        }
      }
    }
  }
}
